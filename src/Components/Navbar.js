import React from 'react';

const Navbar = ({brand}) => {
    return(
        <nax className="navbar navbar-dark bg-dark">
            <div className='container'>
                <a href="#!" className='navbar-brand'>{brand}</a>
            </div>
        </nax>
    )
}

export default Navbar;